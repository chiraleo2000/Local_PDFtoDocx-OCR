# ============================================
# PDF-to-DOCX OCR Pipeline — Dependencies
# v0.1.1  |  Security-hardened, HTML-first, YOLO layout
# ============================================

# Core
gradio>=4.0.0,<5.0.0
huggingface_hub<1.0.0
python-dotenv>=1.0.0

# OCR
pytesseract>=0.3.10

# Image Processing
Pillow>=10.0.0
numpy>=1.24.0
opencv-python-headless>=4.8.0

# PDF Rendering
PyMuPDF>=1.23.0

# Document Export
python-docx>=0.8.11
beautifulsoup4>=4.12.0
lxml>=4.9.0
htmldocx>=0.0.6

# Layout Detection (YOLO)
doclayout-yolo>=0.0.2

# Network
requests>=2.31.0

# ============================================
# OPTIONAL — install manually as needed
# ============================================
# paddleocr>=2.7.0        # Better multilingual OCR
# easyocr>=1.7.0          # Fallback OCR for distorted text

# NOTE: torch is auto-installed by doclayout-yolo.
# For CPU-only (smaller image), install from:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
