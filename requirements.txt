# ============================================
# PDF-to-DOCX OCR Pipeline — Dependencies
# v0.1.0-dev
# ============================================

# Core
gradio>=4.0.0,<5.0.0
huggingface_hub<1.0.0
python-dotenv>=1.0.0

# OCR
pytesseract>=0.3.10

# Image Processing
Pillow>=10.0.0
numpy>=1.24.0
opencv-python-headless>=4.8.0

# PDF Rendering
PyMuPDF>=1.23.0

# Document Export
python-docx>=0.8.11
beautifulsoup4>=4.12.0
lxml>=4.9.0

# Network (for LLM Ollama backend)
requests>=2.31.0

# ============================================
# OPTIONAL — install manually as needed
# ============================================
# paddleocr>=2.7.0        # Better multilingual OCR
# easyocr>=1.7.0          # Fallback OCR for distorted text
# doclayout-yolo>=0.0.2   # AI layout detection
# torch>=2.0.0            # Required for YOLO / EasyOCR / LLM
# torchvision>=0.15.0     # Required for EasyOCR
# transformers>=4.37.0    # Qwen2.5-VL LLM correction
# accelerate>=0.25.0      # LLM quantisation
# bitsandbytes>=0.43.0    # 4-bit quant (Linux only)
