# ============================================
# PDF-to-DOCX OCR Pipeline — Dependencies
# v0.5.0  |  Strict policy: Thai → Thai-TrOCR, other → PaddleOCR
# ============================================

# Core
gradio>=4.0.0
huggingface_hub
python-dotenv>=1.0.0

# Image Processing
Pillow>=10.0.0
numpy>=1.24.0
opencv-python-headless>=4.8.0

# PDF Rendering
PyMuPDF>=1.23.0

# Document Export
python-docx>=0.8.11
beautifulsoup4>=4.12.0
lxml>=4.9.0
htmldocx>=0.0.6

# Layout Detection (Docling primary + YOLO rollback)
docling>=2.25.0
doclayout-yolo>=0.0.2
dill>=0.3.8
lapx>=0.9.4
shapely>=2.0.0

# Network
requests>=2.31.0

# ── OCR Engines (v3.0 strict policy) ──
# Thai pages → Thai-TrOCR (openthaigpt/thai-trocr via transformers/ONNX)
torch>=2.0.0                    # Thai-TrOCR inference (transformers backend)
transformers>=4.37.0            # TrOCR processor + VisionEncoderDecoderModel
onnxruntime>=1.17.0             # optional faster Thai-TrOCR ONNX inference
# Other languages → PaddleOCR PP-OCRv5
paddlepaddle>=2.6.0             # PaddlePaddle framework (required by paddleocr)
paddleocr>=3.1.0                # PP-OCRv5 multilingual + per-line bboxes

# Optional engines — used ONLY when explicitly selected (OCR_ENGINE=<name>)
easyocr>=1.7.0                  # explicit override engine
pytesseract>=0.3.10             # explicit override engine (needs tesseract-ocr system package)
# typhoon-ocr>=0.3.9            # cloud/self-hosted Typhoon OCR — opt-in only

# Testing
pytest>=8.0.0
selenium>=4.15.0

# ============================================
# OPTIONAL — GPU acceleration
# ============================================
# onnxruntime-gpu>=1.17.0 # Thai-TrOCR ONNX GPU inference (replaces onnxruntime)
